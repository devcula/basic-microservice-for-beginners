{
  "name": "basic-microservice-for-beginners",
  "version": "1.0.0",
  "description": "A basic stateless microservice implemented using TypeScript, express, JWT, GraphQL etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc --build",
    "dev": "NODE_ENV=development nodemon",
    "start": "NODE_ENV=production npm run build && node dist/app.js",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@typescript-eslint/eslint-plugin": "^6.11.0",
    "@typescript-eslint/parser": "^6.11.0",
    "eslint": "^8.53.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
